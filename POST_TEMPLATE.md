---
title: 文章标题
published: 2026-06-07
description: 一两句话概括这篇文章的内容，会显示在首页列表卡片上
tags: [标签1, 标签2]
category: 分类名
draft: false
---

> ⚠️ Frontmatter 硬性规则（不遵守会导致构建失败，全站无法更新）：
> 1. `title` / `description` 中如果包含**英文冒号（`:`）+ 空格**、双引号、单引号、`#`、`[`、`]` 等特殊字符，**必须用双引号包裹**整个值，内部双引号转义为 `\"`。
> 2. 例：`title: "Kimi K3: 2.8万亿参数的开源模型把\"最大\"戴稳了"`
> 3. 没有特殊字符时可不加引号，但加了也不会错。
> 4. 写完 frontmatter 后自检一遍：`title` 值中是否有「冒号+空格」却未加引号。

# 正文大标题（可选）

这里写正文内容。支持完整的 Markdown 语法。

## 二级标题

段落之间空一行。

**加粗文字** 和 *斜体文字* 都可以用。

## 插入图片

把图片放到 `src/content/posts/文章文件夹名/` 目录下，然后用相对路径引用：

![图片描述](./image.jpg)

## 插入代码

```python
print("Hello World")
```

## 插入链接

[链接文字](https://example.com)

## 插入引用

> 这是一段引用文字，适合用来放名言或重点提示。

## 插入列表

- 无序列表项 1
- 无序列表项 2

1. 有序列表项 1
2. 有序列表项 2
